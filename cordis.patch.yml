# dsh-time-gate bundle patch: 把宿主插件行插入 web profile roster。
# 安装后必须把 config.repoDir 改成你本机仓库的绝对路径
# （gate.js 等脚本 + config.json + queue.json 所在目录）。
- insert:
    - id: time-gate
      name: 'dsh-time-gate'
      config:
        repoDir: 'C:\path\to\your\time-gate'
